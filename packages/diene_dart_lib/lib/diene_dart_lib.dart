/// The generic, replaceable sample surface of the Diene pure-Dart library
/// template: a [Note] value type with a JSON wire codec and a pure summariser.
///
/// A materialized child replaces `lib/src/**` wholesale with its own domain
/// and re-points these exports; the surrounding machinery is unchanged.
library;

export 'src/note.dart';
export 'src/note_summary.dart';
